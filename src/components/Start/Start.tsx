import { Logo } from "../../assets";
import { S } from "./style";

const StartButton = () => {
  return <></>;
};

const Start = () => {
  return (
    <S.TotalWrapper>
      <S.Logo src={Logo} />
    </S.TotalWrapper>
  );
};

export default Start;
